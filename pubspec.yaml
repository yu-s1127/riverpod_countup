name: riverpod_coutup
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.16.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_riverpod: ^1.0.3
  freezed_annotation: ^1.1.0

dev_dependencies:
  flutter_lints: ^1.0.4

  build_runner: ^2.1.10
  flutter_test:
    sdk: flutter
  freezed: ^1.1.0
  test: ^1.19.5
  json_serializable: ^6.2.0

flutter:
  uses-material-design: true
